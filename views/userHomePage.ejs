<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/output.css">
    <title>Home Page</title>
</head>
<body class="text-zinc-200 bg-gray-600 h-screen">
    <%- include("appHeader", {user: locals.user}) %>
    <main class="min-h-screen bg-gray-900 p-3">
        <% if (locals.user) { %>
            <h1 class="text-2xl font-bold mb-3">Hi <%= locals.user.firstname + " " + locals.user.lastname %></h1>
        <% } %>
        <div class="flex items-center justify-between gap-6 mb-3">
            <h1 class="text-2xl font-bold">Your Folders</h1>
            <a href="/home/create-folder">
                <button class="px-3 py-2 rounded-full border-2 border-white hover:bg-white hover:text-black">Create Folder</button>
            </a>
        </div>
        <section class="min-h-screen grid grid-cols-[repeat(auto-fit,minmax(250px,1fr))] gap-4 auto-rows-[250px] text-black">

        </section>
  
    </main>
</body>
</html>